import React from 'react';
import { Col, Row } from 'react-bootstrap';

const Settings = subtitle => props => {
  return (
    <Row>
      <Col>
      </Col>
    </Row>
  );
};

export default Settings;